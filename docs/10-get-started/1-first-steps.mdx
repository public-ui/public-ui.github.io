import { KolAlert, KolButton, KolHeading } from '@public-ui/react-v19';

# Erste Schritte

<KolAlert _type="info" _variant="card">
	Für ITZBund-Projekte gibt es ein vorkonfiguriertes Seed-Projekt über die internen Kommunikationswege.
</KolAlert>

## Schnellstart: Neues Projekt

Der schnellste Weg zu starten ist der Kommandozeilenassistent:

```bash
npm init kolibri@latest my-kolibri-app
```

<img
	src="https://raw.githubusercontent.com/public-ui/.github/main/profile/create-kolibri.gif"
	alt="Zeigt wie man mit create-kolibri eine neue App anlegen kann."
/>

Der Assistent erstellt ein vollständig konfiguriertes Projekt mit:
- ✅ Komponenten und Theme bereits installiert
- ✅ Framework Ihrer Wahl (React, Angular, Vue, etc.)
- ✅ Entwicklungsserver vorkonfiguriert
- ✅ Beispiel-Komponenten zum Loslegen

## Integration in bestehendes Projekt

### In 3 Schritten zu KoliBri

#### 1️⃣ Pakete installieren

<kol-tabs _headers="['npm', 'pnpm', 'yarn']" _tabs='[{"_label":"NPM"},{"_label":"PNPM"},{"_label":"YARN"}]'>
	<div>

```bash
npm i @public-ui/components @public-ui/theme-default
```

</div>
	<div>

```bash
pnpm i @public-ui/components @public-ui/theme-default
```

</div>
	<div>

```bash
yarn add @public-ui/components @public-ui/theme-default
```

</div>
</kol-tabs>

Für Framework-Adapter (React, Angular, Vue, etc.) installieren Sie zusätzlich das entsprechende Paket:
- React: `@public-ui/react-v19`
- Angular: `@public-ui/angular-v19` (oder v20/v21)
- Vue: `@public-ui/vue`
- [Weitere Frameworks →](./frameworks)

#### 2️⃣ Komponenten registrieren

Rufen Sie `register()` beim Start Ihrer App auf:

```ts
import { register } from '@public-ui/components';
import { defineCustomElements } from '@public-ui/components/dist/loader';
import { DEFAULT } from '@public-ui/theme-default';

await register(DEFAULT, defineCustomElements);
```

**Framework-spezifisch?** → [Detaillierte Anleitungen für alle Frameworks](./frameworks)

#### 3️⃣ Komponenten verwenden

Jetzt können Sie KoliBri-Komponenten nutzen:

**React/TypeScript:**
```tsx
import { KolButton, KolHeading } from '@public-ui/react-v19';

export const App = () => (
  <div>
    <KolHeading _label="Hallo KoliBri!" _level={1} />
    <KolButton _label="Klick mich" _on={{ onClick: () => alert('👋') }} />
  </div>
);
```

**HTML/JavaScript:**
```html
<kol-heading _label="Hallo KoliBri!" _level="1"></kol-heading>
<kol-button _label="Klick mich"></kol-button>
```

## Nächste Schritte

<div style={{ display: 'grid', gap: '1rem', marginTop: '2rem' }}>
	<KolAlert _type="success" _variant="card" _heading="🎨 Playground">
		[Probieren Sie KoliBri direkt im Browser aus](./playground) – ohne Installation!
	</KolAlert>

	<KolAlert _type="info" _variant="card" _heading="🛠️ Framework-Integration">
		[Detaillierte Anleitungen für React, Angular, Vue und mehr](./frameworks)
	</KolAlert>

	<KolAlert _type="default" _variant="card" _heading="📚 Komponenten">
		[Entdecken Sie 50+ barrierefreie Komponenten](/docs/components)
	</KolAlert>
</div>

## Häufige Fragen

<details>
<summary><strong>Komponenten werden nicht angezeigt?</strong></summary>

Stellen Sie sicher, dass `register()` **vor** dem Rendern aufgerufen wurde und das Promise aufgelöst ist.

```tsx
// ✅ Richtig
register(DEFAULT, defineCustomElements)
  .then(() => {
    // Hier App rendern
  });

// ❌ Falsch
register(DEFAULT, defineCustomElements); // Promise nicht abgewartet
// App wird sofort gerendert
```
</details>

<details>
<summary><strong>Welches Theme soll ich verwenden?</strong></summary>

- **`@public-ui/theme-default`** – Neutrales Standard-Theme (empfohlen für den Start)
- **`@public-ui/theme-ecl`** – European Commission Library Theme
- [Eigenes Theme erstellen](/docs/concepts/styling/theming)
</details>

<details>
<summary><strong>TypeScript-Fehler bei Properties?</strong></summary>

Prüfen Sie:
1. `"moduleResolution": "Node"` in `tsconfig.json`
2. Framework-Adapter installiert (z.B. `@public-ui/react-v19`)
3. Komponenten aus dem Adapter importieren (nicht aus `@public-ui/components`)
</details>

<details>
<summary><strong>Kann ich KoliBri ohne Build-Tool nutzen?</strong></summary>

Ja! Laden Sie KoliBri per CDN:

```html
<script type="module">
  import { register } from 'https://unpkg.com/@public-ui/components@latest/dist/esm/index.js';
  import { defineCustomElements } from 'https://unpkg.com/@public-ui/components@latest/dist/loader/index.js';
  import { DEFAULT } from 'https://unpkg.com/@public-ui/theme-default@latest/dist/index.mjs';

  register(DEFAULT, defineCustomElements);
</script>
```

⚠️ **Für Produktion:** Nutzen Sie fixe Versionen statt `@latest`
</details>

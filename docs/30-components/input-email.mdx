---
title: InputEmail
description: Beschreibung, Spezifikation und Beispiele für die InputEmail-Komponente.
---

import Readme from '@site/readmes/input-email/readme.md';
import InputEmailPreview from '@site/src/components/previews/components/InputEmail';
import BetaDocsBanner from '@site/src/components/BetaDocsBanner';
import LabelNote from './_shared/a11y-properties/label.md';
import ErrorMessageNote from './_shared/a11y-properties/error-message.md';
import SuggestionsNote from './_shared/a11y-properties/suggestions.md';
import CharacterCounterNote from './_shared/a11y-properties/character-counter.md';
import NativeKeyboardIntro from './_shared/keyboard/native-intro.md';
import BasicFeature from './_shared/features/basic.md';
import FormAttributesFeature from './_shared/features/form-attributes.md';
import DisabledProp from './_shared/feature-properties/disabled.md';
import ReadOnlyProp from './_shared/feature-properties/read-only.md';
import RequiredProp from './_shared/feature-properties/required.md';
import HintAndMsgFeature from './_shared/features/hint-and-msg.md';
import CharacterCounterFeature from './_shared/features/character-counter.md';
import PlaceholderFeature from './_shared/features/placeholder.md';
import HideLabelFeature from './_shared/features/hide-label.md';
import IconsFeature from './_shared/features/icons.md';
import SmartButtonFeature from './_shared/features/smart-button.md';
import SuggestionsFeature from './_shared/features/suggestions.md';
import SubmitOnEnterNote from './_shared/keyboard/submit-on-enter.md';
import EventsIntro from './_shared/api/events-intro.md';


# InputEmail


<BetaDocsBanner />

**Synonyme:** E-Mail-Eingabefeld, E-Mail-Feld, Email Input, Email Field

**Beschreibung:** Mit **InputEmail** können eine oder mehrere E-Mail-Adressen eingegeben und bearbeitet werden.

Die Komponente basiert auf dem nativen HTML5-Eingabetyp `email` und profitiert dadurch von dessen Semantik, der browserseitigen Formatprüfung sowie der standardisierten Unterstützung durch Browser und assistive Technologien. Mobile Browser bieten für diesen Eingabetyp in der Regel eine angepasste Bildschirmtastatur an.

## Beispiel

Standard-Eingabefeld für E-Mail-Adressen ohne optional gesetzte Felder:

<InputEmailPreview context="example" />

## Barrierefreiheit

* <LabelNote />
* <ErrorMessageNote />
* <SuggestionsNote />
* <CharacterCounterNote />
* Die native Formatprüfung des Eingabetyps `email` wirkt sich nicht auf die Fehlerdarstellung der Komponente aus. Fehlermeldung und `aria-invalid` werden ausschließlich über `_msg` in Verbindung mit `_touched` gesteuert. Werten Sie die Validierung daher in der Anwendung aus und stellen Sie eine verständliche Fehlermeldung über `_msg` bereit.
* `_multiple` wird unverändert als natives Attribut `multiple` übergeben. KoliBri ergänzt keine eigene Ansage, dass mehrere Adressen erwartet werden. Weisen Sie über die Beschriftung oder `_hint` darauf hin, dass mehrere Adressen durch Kommas getrennt eingegeben werden können.

### Konkrete Designentscheidungen

| Entscheidung | Begründung |
| ------------ | ---------- |
| Verwendung des nativen HTML5-Eingabetyps `email` | Die Komponente nutzt bewusst den nativen HTML5-Eingabetyp und profitiert dadurch von dessen standardisierter Unterstützung durch Browser und assistive Technologien. |
| Keine automatische Übernahme der nativen Formatprüfung | Der Fehlerzustand wird ausschließlich über `_msg` und `_touched` gesteuert. So bestimmt die Anwendung Zeitpunkt und Wortlaut der Fehlermeldung, und Fehler werden in allen Formular-Komponenten einheitlich dargestellt und mit dem Feld verknüpft. |
| `_multiple` und `_pattern` als native Attribute | Die Prüfung mehrerer, durch Kommas getrennter Adressen sowie zusätzlicher Formatvorgaben übernimmt der Browser. Die Komponente teilt den Wert nicht selbst auf. |
| Verzögerte Ansage des Zeichenzählers | Die Live-Region des Zeichenzählers wird erst 1000 ms nach der letzten Eingabe aktualisiert, damit Screenreader die Eingabe nicht durch fortlaufende Ansagen unterbrechen. |

### Links und Referenzen

- <kol-link _href="https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input/email" _target="_blank" _label="MDN Web Docs: Input Type Email"></kol-link>
- <kol-link _href="https://html.spec.whatwg.org/multipage/input.html#email-state-(type=email)" _target="_blank" _label="HTML Living Standard: The input element - type email"></kol-link>
- <kol-link _href="https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/datalist" _target="_blank" _label="MDN Web Docs: Datalist"></kol-link>

## Verwendung

- Verwenden Sie `_multiple`, um die Eingabe mehrerer, durch Kommas getrennter E-Mail-Adressen zu ermöglichen.
- Nutzen Sie `_pattern`, wenn zusätzlich zur nativen E-Mail-Validierung weitere Formatvorgaben erforderlich sind.
- Nutzen Sie `_maxLength`, um die maximal zulässige Anzahl an Zeichen zu begrenzen, und legen Sie mit `_maxLengthBehavior` fest, ob die Grenze nativ durchgesetzt (`'hard'`, Standard) oder lediglich angezeigt wird (`'soft'`).
- Setzen Sie `_autoComplete` auf `'email'`, wenn der Browser gespeicherte E-Mail-Adressen vorschlagen soll. Der Standardwert ist `'off'`.

**Hinweis:** Die native Formatprüfung des HTML5-Eingabetyps `email` erfolgt unabhängig von `_msg` und `_touched`. Sollen Validierungsfehler in der Komponente angezeigt werden, müssen diese durch die Anwendung ausgewertet und über `_msg` bereitgestellt werden.

### Tastatursteuerung

<NativeKeyboardIntro />

| Taste                         | Funktion                                                                                 |
| ----------------------------- | ---------------------------------------------------------------------------------------- |
| `Tab`                         | Fokus auf das Eingabefeld bzw. das nächste fokussierbare Element setzen.                 |
| `Shift+Tab`                   | Fokus auf das vorherige fokussierbare Element setzen.                                    |
| `Pfeil-Tasten (links/rechts)` | Cursor innerhalb des Eingabewerts bewegen.                                               |
| `Pfeil-Tasten (oben/unten)`   | Bei gesetzten Vorschlagswerten (`_suggestions`): Vorschlagsliste öffnen und durchlaufen. |
| `Pos1` / `Ende`               | Cursor an den Anfang bzw. das Ende des Eingabewerts setzen.                              |
| `Enter`                       | Umgebendes Formular absenden.                                                            |

<SubmitOnEnterNote />

### Best Practices / Empfehlungen

- Verlassen Sie sich nicht ausschließlich auf die native Formatprüfung des Browsers. Werten Sie Validierungsfehler in der Anwendung aus und stellen Sie über `_msg` zusammen mit `_touched` eine aussagekräftige Fehlermeldung bereit.
- Formulieren Sie Fehlermeldungen konkret und lösungsorientiert, damit Nutzende erkennen können, wie die eingegebene E-Mail-Adresse korrigiert werden muss.
- Verwenden Sie `_pattern` nur für fachlich notwendige zusätzliche Anforderungen, beispielsweise wenn ausschließlich E-Mail-Adressen einer bestimmten Domain zulässig sind. Beschreiben Sie diese Anforderung zusätzlich über `_hint`.
- Weisen Sie bei Verwendung von `_multiple` deutlich darauf hin, dass mehrere E-Mail-Adressen durch Kommas getrennt eingegeben werden müssen.
- Setzen Sie `_autoComplete` passend zum Anwendungsfall ein, beispielsweise mit dem Wert `'email'`, damit Browser die Eingabe unterstützen können.
- Verwenden Sie `_placeholder` ausschließlich als zusätzliche Orientierung und nicht als Ersatz für eine aussagekräftige Beschriftung (`_label`).

### Anwendungsfälle

- Eingabe einer E-Mail-Adresse in Kontakt-, Registrierungs- oder Anmeldeformularen
- Erfassung einer geschäftlichen oder privaten E-Mail-Adresse in Benutzerprofilen
- Eingabe einer Empfängeradresse für Benachrichtigungen oder den Versand von Informationen
- Erfassung mehrerer E-Mail-Adressen, beispielsweise für Einladungen oder Verteiler
- Eingabe einer E-Mail-Adresse zur Anmeldung für Newsletter oder andere Mitteilungen

### FAQ

**Wie kann ich Validierungsfehler barrierefrei anzeigen?**  
Die native Formatprüfung des HTML5-Eingabetyps `email` ersetzt keine barrierefreie Fehlermeldung. Werten Sie das Ergebnis der Validierung in Ihrer Anwendung aus und stellen Sie Validierungsfehler über `_msg` zusammen mit `_touched` bereit. Die Meldung wird dann automatisch mit dem Feld verknüpft und das Feld als ungültig gekennzeichnet.

**Wie werden mehrere E-Mail-Adressen bei `_multiple` verarbeitet?**  
Der Wert des Eingabefelds enthält weiterhin den vollständigen, durch Kommas getrennten String. Eine automatische Aufteilung in einzelne E-Mail-Adressen erfolgt nicht durch die Komponente.

**Werden internationalisierte (Unicode-)E-Mail-Adressen unterstützt?**  
Die Komponente nutzt die native Browservalidierung des HTML5-Eingabetyps `email`. Ob internationalisierte E-Mail-Adressen unterstützt werden, hängt daher vom jeweiligen Browser ab.

**Kann ich die native E-Mail-Validierung erweitern?**  
Ja. Nutzen Sie `_pattern`, wenn zusätzlich zur nativen E-Mail-Validierung weitere fachliche Anforderungen geprüft werden sollen, beispielsweise um E-Mail-Adressen auf bestimmte Domains einzuschränken. Bei gesetztem `_multiple` prüft der Browser das Muster für jede einzelne Adresse.

## Playground

Testen Sie die verschiedenen Eigenschaften des InputEmail-Feldes:

<InputEmailPreview context="playground" />

## Funktionsbeispiele

### Einfaches Eingabefeld

<BasicFeature />

<InputEmailPreview
	context="feature"
	visibleProperties={['_label']}
	initialProps={{
		_label: 'E-Mail-Adresse',
	}}
/>

### Formularattribute

<FormAttributesFeature />

- <DisabledProp />
- <ReadOnlyProp />
- <RequiredProp />

Die Properties werden 1:1 auf die gleichnamigen nativen HTML-Attribute des `input`-Elements übertragen.

<InputEmailPreview
	context="feature"
	visibleProperties={['_disabled', '_readOnly', '_required']}
	initialProps={{
		_label: 'E-Mail-Adresse',
	}}
/>

### Hinweistexte und Fehlermeldungen

<HintAndMsgFeature />

<InputEmailPreview
	context="feature"
	visibleProperties={['_hint', '_msg', '_touched', '_hideMsg']}
	initialProps={{
		_label: 'E-Mail-Adresse',
		_hint: 'Wir verwenden Ihre E-Mail-Adresse nur für Benachrichtigungen.',
		_msg: { _description: 'Bitte geben Sie eine gültige E-Mail-Adresse ein, z. B. name@beispiel.de.' },
		_touched: true,
		_hideMsg: false,
	}}
/>

### Mehrere E-Mail-Adressen

Mit `_multiple` akzeptiert das Feld mehrere, durch Kommas getrennte E-Mail-Adressen. Die Eigenschaft wird als natives Attribut `multiple` übergeben; der Browser prüft dann jede einzelne Adresse. Der Wert des Feldes bleibt ein einzelner, kommagetrennter String. Weisen Sie Nutzende über `_hint` auf die erwartete Schreibweise hin.

<InputEmailPreview
	context="feature"
	visibleProperties={['_multiple']}
	initialProps={{
		_label: 'E-Mail-Adressen',
		_multiple: true,
		_hint: 'Mehrere Adressen durch Kommas trennen',
	}}
/>

### Zusätzliche Formatvorgaben

Mit `_pattern` hinterlegen Sie einen regulären Ausdruck, dem die Eingabe zusätzlich zur nativen E-Mail-Prüfung entsprechen muss – beispielsweise, um nur Adressen einer bestimmten Domain zuzulassen. Die Eigenschaft wird als natives Attribut `pattern` übergeben. Die Komponente zeigt das Prüfergebnis nicht selbst an; Fehlermeldungen stellen Sie über `_msg` bereit.

<InputEmailPreview
	context="feature"
	visibleProperties={['_pattern']}
	initialProps={{
		_label: 'Dienstliche E-Mail-Adresse',
		_pattern: '.+@beispiel\\.de',
		_hint: 'Nur Adressen der Domain beispiel.de',
	}}
/>

### Zeichenbegrenzung und Zeichenzähler

<CharacterCounterFeature />

<InputEmailPreview
	context="feature"
	visibleProperties={['_hasCounter', '_maxLength', '_maxLengthBehavior']}
	initialProps={{
		_label: 'E-Mail-Adresse',
		_hasCounter: true,
		_maxLength: 100,
	}}
/>

### Autovervollständigung

Mit `_autoComplete` steuern Sie, ob der Browser gespeicherte E-Mail-Adressen vorschlagen darf. Der Standardwert ist `'off'`. Für Felder, bei denen die Browser-Autovervollständigung erwünscht ist, setzen Sie den Autocomplete-Token `'email'`.

<InputEmailPreview
	context="feature"
	visibleProperties={['_autoComplete']}
	initialProps={{
		_label: 'E-Mail-Adresse',
		_autoComplete: 'email',
	}}
/>

### Placeholder

<PlaceholderFeature />

<InputEmailPreview
	context="feature"
	visibleProperties={['_placeholder']}
	initialProps={{
		_label: 'E-Mail-Adresse',
		_placeholder: 'z. B. name@beispiel.de',
	}}
/>

### Label ausblenden

<HideLabelFeature />

<InputEmailPreview
	context="feature"
	visibleProperties={['_label', '_hideLabel']}
	initialProps={{
		_label: 'E-Mail-Adresse',
		_hideLabel: false,
	}}
/>

### Icons

<IconsFeature />

<InputEmailPreview
	context="feature"
	visibleProperties={['_icons']}
	initialProps={{
		_label: 'E-Mail-Adresse',
		_icons: {
			left: 'kolicon-kolibri',
		},
	}}
/>

### SmartButton

<SmartButtonFeature />

<InputEmailPreview
	context="feature"
	visibleProperties={['_smartButton']}
	initialProps={{ _label: 'Mit SmartButton', _placeholder: 'Platzhalter', _smartButton: { _label: 'Action' } }}
/>

### Vorschlagswerte (Suggestions)

<SuggestionsFeature />

<InputEmailPreview
	context="feature"
	visibleProperties={['_suggestions']}
	initialProps={{
		_label: 'E-Mail-Adresse',
		_suggestions: ['info@beispiel.de', 'kontakt@beispiel.de', 'service@beispiel.de'],
	}}
/>

## API

### Events

<EventsIntro />

| Event     | Auslöser                           | Value                           |
| --------- | ---------------------------------- | ------------------------------- |
| `click`   | Eingabefeld wird angeklickt        | -                               |
| `focus`   | Eingabefeld wird fokussiert        | -                               |
| `blur`    | Eingabefeld verliert Fokus         | -                               |
| `keydown` | Taste wird im Eingabefeld gedrückt | -                 |
| `input`   | Wert wird durch Eingabe geändert   | Aktueller Wert des Eingabefelds |
| `change`  | Eingabe wurde abgeschlossen        | Aktueller Wert des Eingabefelds |

<Readme />

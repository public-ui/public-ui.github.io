declare type Hour = `${number}`;
declare type Minute = `${number}`;
declare type Second = `${number}`;
declare type Year = `${number}`;
declare type Month = `${number}`;
declare type Day = `${number}`;
declare type CalendarWeek = `${number}`;
declare type IsoDate = `${Year}-${Month}-${Day}`;
declare type IsoTime = `${Hour}:${Minute}:${Second}` | `${Hour}:${Minute}:${Second}.${number}`;
declare type IsoLocalDateTime = `${IsoDate}T${IsoTime}`;
declare type IsoMonth = `${Year}-${Month}`;
declare type IsoWeek = `${Year}-W${CalendarWeek}`;
export declare type Iso8601 = IsoDate | IsoTime | IsoLocalDateTime | IsoMonth | IsoWeek;
export {};
